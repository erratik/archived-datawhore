import { OrderDropByPipe } from './order-drop-by.pipe';

describe('OrderDropByPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderDropByPipe();
    expect(pipe).toBeTruthy();
  });
});
