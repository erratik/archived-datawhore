import { OrderDropByPipe } from './order-by-field-path.pipe';

describe('OrderByFieldPathPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderDropByPipe();
    expect(pipe).toBeTruthy();
  });
});
