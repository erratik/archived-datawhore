import { PopulateMatchesPipe } from './populate-matches.pipe';

describe('PopulateMatchesPipe', () => {
  it('create an instance', () => {
    const pipe = new PopulateMatchesPipe();
    expect(pipe).toBeTruthy();
  });
});
