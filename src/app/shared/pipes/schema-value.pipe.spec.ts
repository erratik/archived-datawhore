import { SchemaValuePipe } from './schema-value.pipe';

describe('SchemaValuePipe', () => {
  it('create an instance', () => {
    const pipe = new SchemaValuePipe();
    expect(pipe).toBeTruthy();
  });
});
